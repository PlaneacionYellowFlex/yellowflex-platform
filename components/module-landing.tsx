import type { LucideIcon } from "lucide-react";
import { Clock3, Database, ShieldCheck } from "lucide-react";
import PageHeader from "@/components/page-header";

export default function ModuleLanding({ title, description, icon: Icon, capabilities, integrationNote }: { title: string; description: string; icon: LucideIcon; capabilities: string[]; integrationNote?: string }) { return <div className="space-y-8"><PageHeader eyebrow="Módulo corporativo" title={title} description={description} /><section className="overflow-hidden rounded-2xl border border-slate-200 bg-white"><div className="grid lg:grid-cols-[1.1fr_.9fr]"><div className="p-6 sm:p-8"><span className="flex size-12 items-center justify-center rounded-xl bg-[#0b3a82] text-white"><Icon className="size-6" /></span><h2 className="mt-6 text-2xl font-bold tracking-tight text-slate-900">Base lista para construir</h2><p className="mt-3 max-w-xl text-sm leading-6 text-slate-500">Este espacio define la experiencia, el alcance funcional y los puntos de integración. Las operaciones y los datos se conectarán a servicios seguros en su sprint correspondiente.</p><div className="mt-7 grid gap-3 sm:grid-cols-2">{capabilities.map((capability) => <div key={capability} className="rounded-xl border border-slate-100 bg-slate-50 px-4 py-3 text-sm font-medium text-slate-700">{capability}</div>)}</div></div><aside className="bg-[#0b3a82] p-6 text-white sm:p-8"><p className="text-xs font-bold uppercase tracking-[.18em] text-[#f4b400]">Estado del módulo</p><div className="mt-6 space-y-5"><div className="flex gap-3"><Clock3 className="mt-0.5 size-5 shrink-0 text-[#f4b400]" /><div><p className="font-semibold">Planeación funcional</p><p className="mt-1 text-sm leading-5 text-blue-100">Landing y estructura preparadas para el siguiente sprint.</p></div></div><div className="flex gap-3"><Database className="mt-0.5 size-5 shrink-0 text-[#f4b400]" /><div><p className="font-semibold">Datos pendientes</p><p className="mt-1 text-sm leading-5 text-blue-100">Sin datos ficticios ni simulación de backend.</p></div></div><div className="flex gap-3"><ShieldCheck className="mt-0.5 size-5 shrink-0 text-[#f4b400]" /><div><p className="font-semibold">Acceso por rol</p><p className="mt-1 text-sm leading-5 text-blue-100">Se conectará a autorización validada en servidor.</p></div></div></div>{integrationNote && <p className="mt-7 border-t border-white/15 pt-5 text-sm leading-6 text-blue-100">{integrationNote}</p>}</aside></div></section></div>; }
