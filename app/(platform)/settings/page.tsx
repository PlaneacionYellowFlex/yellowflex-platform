import { ShieldCheck } from "lucide-react";
import PageHeader from "@/components/page-header";
import { platformRoles } from "@/lib/access-control";
export default function SettingsPage() { return <div className="space-y-8"><PageHeader title="Configuración" description="Preparación de la administración corporativa. Las opciones se habilitarán junto con autenticación y autorización reales." /><section className="rounded-2xl border border-slate-200 bg-white p-6 sm:p-8"><div className="flex gap-4"><span className="flex size-10 shrink-0 items-center justify-center rounded-xl bg-blue-50 text-[#0b3a82]"><ShieldCheck className="size-5" /></span><div><h2 className="text-lg font-bold text-slate-900">Roles preparados para autorización</h2><p className="mt-1 max-w-2xl text-sm leading-6 text-slate-500">Estos contratos de dominio no habilitan acceso en el cliente. La validación se implementará en servidor con el proveedor de identidad elegido.</p><div className="mt-5 flex flex-wrap gap-2">{platformRoles.map((role) => <span key={role} className="rounded-lg border border-slate-200 bg-slate-50 px-3 py-2 text-xs font-semibold text-slate-600">{role}</span>)}</div></div></div></section></div>; }
